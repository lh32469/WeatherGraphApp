spring:
  application:
    name: weathergraphapp-${BRANCH:dev}
#  cloud:
#    consul:
#      host: dell-4290.local
#      port: 8500
#      enabled: true
#      discovery:
#        metadata:
#          f1: "v1"
#          f2: "v2"
#        instance-id: "${spring.application.name}:${random.int[1,999999]}"
#        prefer-ip-address: true
#        health-check-path: "/${BRANCH:dev}/actuator/health"
